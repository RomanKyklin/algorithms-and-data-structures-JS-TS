/**
 * @param {Array} arr
 * @return {Array}
 */
export const insertionSort = (arr: number[]): number[] => {
    const result = [...arr];
    let currentSortedIndex = 0;
    let nextIndex = currentSortedIndex + 1;
    let isSorted = false;

    do {

    } while (isSorted)

    return result;
}
